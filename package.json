{
  "name": "autocomplete",
  "version": "0.1.0",
  "description": "autocomplete component",
  "main": "index.js",
  "scripts": {
    "lint:init" : "./node_modules/.bin/eslint --init",
    "lint" : "./node_modules/.bin/eslint ./src/autocomplete.js --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "fruit1122",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^5.16.0"
  }
}
